<?php
/**
 * API.IR WordPress SDK
 * تنظیمات — تنها فایلی که برای شروع کار باید ویرایش کنید
 * (یا این ثابت‌ها را در wp-config.php تعریف کنید تا با به‌روزرسانی SDK از بین نروند)
 *
 * @package  APIIR
 * @version  1.0.0
 * @link     https://api.ir
 */

defined('ABSPATH') || exit;

// کلید خود را از پنل کاربری https://p.api.ir بسازید
if (!defined('APIIR_TOKEN')) {
    define('APIIR_TOKEN', 'YOUR_API_TOKEN');
}

// مهلت سراسری پاسخ به ثانیه — پیش‌فرض سرویس‌های سبک.
// هر سرویس پیش‌فرض خودش را دارد (سرویس‌های حجیم بیشتر) و در هر فراخوانی
// با آرگومان $timeout قابل تغییر است.
if (!defined('APIIR_TIMEOUT')) {
    define('APIIR_TIMEOUT', 30);
}

// اعتبارسنجی SSL — فقط اگر سرور شما خطای گواهی می‌دهد این را false کنید.
// هشدار: خاموش کردن این گزینه امنیت ارتباط با s.api.ir را از بین می‌برد؛
// راه‌حل درست، به‌روزرسانی گواهی‌های هاست است.
// این تنظیم سراسری است و برای همه‌ی سرویس‌ها به‌صورت یکسان اعمال می‌شود.
if (!defined('APIIR_SSL_VERIFY')) {
    define('APIIR_SSL_VERIFY', true);
}

// آدرس سرویس ثابت است و در فایل‌های پوشه services تعریف شده است
